# requirements-all.txt
# Consolidated requirements for NightChronicles — install to get all runtime deps for all modules.
# Purpose: one-shot install for development environment. Keep this file updated when module requirements change.

# Core / common
python-dotenv>=1.0.0
pyyaml>=6.0
filelock
httpx>=0.24.0
aiofiles>=23.2.1
aiohttp>=3.9.0
backoff>=2.2.1
pydantic>=2.5.0
structlog>=24.1.0
jsonschema>=4.17.0

# LLM / API clients
openai>=1.6.0
anthropic>=0.18.0

# TTS / audio
elevenlabs>=2.0.0

# Security / system
cryptography>=41.0.0
keyring>=24.0.0
psutil>=5.9.0

# GUI
PySide6>=6.6

# Narration / NLP / utilities
questionary>=2.0.1
num2words>=0.5.13
spacy>=3.7.0

# Optional niceties (logging/display)
colorama>=0.4.6
rich>=13.7.0

# Development & testing (optional) - install with: pip install -r requirements-all-dev.txt
# Keep dev/test deps separated to avoid bloating production installs
