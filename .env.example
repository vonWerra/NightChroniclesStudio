# Global outputs root (parent of module subfolders outline/prompts/narration/postprocess/final/tts/export)
NC_OUTPUTS_ROOT=

# OpenAI credentials (required for narrationbuilder and other GPT-based steps)
OPENAI_API_KEY=

# GPT defaults (can be overridden in GUI tabs)
GPT_MODEL=gpt-5
GPT_TEMPERATURE=0.4

# Optional explicit roots (override NC_OUTPUTS_ROOT/<subdir>)
OUTLINE_OUTPUT_ROOT=
PROMPTS_OUTPUT_ROOT=
NARRATION_OUTPUT_ROOT=
POSTPROC_OUTPUT_ROOT=
FINAL_OUTPUT_ROOT=
TTS_OUTPUT_ROOT=
EXPORT_OUTPUT_ROOT=
